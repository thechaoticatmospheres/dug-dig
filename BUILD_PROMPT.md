Build the browser game described in PLAN.md in this Dug Dig workspace. Execute the work, including implementation, testing and deployment, rather than stopping at another plan.

Use the original 1982 arcade Dig Dug as the fidelity target, plus simultaneous 2–4 player online cooperative AND competitive modes. The arcade version and both online modes are confirmed requirements. Preserve an Arcade mode with solo and original alternating two-player play. Keep multiplayer rules separate from arcade behavior.

First inspect this repository and applicable instructions. Inspect the existing Zombiino project at C:/Users/joshu/OneDrive/Documents/ChatGPT/zombiino for its GitHub Pages and free Render deployment patterns. Build this as a separate game with its own backend.

Before implementing detailed mechanics, lock a specific arcade revision and cabinet settings, and create docs/reference with sources, a fidelity checklist and measured values. Research authoritative manuals and reference gameplay. Match movement and digging speeds, turn alignment, pumping/inflation/deflation, Pooka and Fygar behavior, ghosting, fire, rock support and falling, depth/direction/rock scoring, bonus items, authored round layouts, difficulty, lives, death resets and final-enemy escape. Mark unknowns and deviations honestly. Do not mix values from different ports or claim exact fidelity without comparison.

Reproduce the original portrait composition, pixel proportions, palette, tunnel shapes, sprites, animation timing, arcade HUD and audio behavior as closely as the verified reference permits. Create an organized asset manifest and record sources. Do not leave placeholder art or missing core audio in the finished build. Avoid modern visual effects that change the arcade presentation. Provide keyboard and gamepad controls, volume settings and responsive scaling without stretching.

Use Phaser + TypeScript + Vite for the browser. Keep a deterministic, fixed-step TypeScript simulation separate from rendering so Arcade mode runs locally and the Node.js multiplayer server runs the same rules. Use an authoritative WebSocket server with ws. Clients send inputs; the server owns terrain, enemies, pumps, rocks, damage, score, lives and round transitions. Add local movement prediction/reconciliation, remote interpolation, terrain revisions and complete state restoration on rejoin.

For cooperative play, implement private rooms, codes and invite links, 2–4 player slots, distinct colors and player labels, ready/start flow, team score, personal lives, safe respawns, elimination/spectating and game over. Players share one board and pass through each other. Pumps target enemies with deterministic single-player attachment ownership; rocks can crush players. Keep balancing data separate from Arcade mode. Specify safe spawn selection and simultaneous-action handling. Late joins wait for the next round, while reconnects reclaim their existing slot. Transfer lobby ownership when the host leaves and let the server continue the match. Use the precise rules in PLAN.md and document any necessary refinements.

For competitive play, implement the shared-board score race in PLAN.md: five cleared rounds or an early finish when everyone is eliminated, three starting lives each, highest accumulated score wins and tied scores share a placing. Award pump kills to the attachment owner at the pop, rock-chain points to the player who removed the rock's final support, and bonus items to the first authoritative collector. Record rock ownership at the trigger and resolve simultaneous actions deterministically. Players cannot pump each other, but rocks threaten everyone without awarding points for player deaths. Eliminated players spectate, new arrivals wait for the next match, reconnects reclaim existing slots, and the results screen supports rematches. Show individual scores, lives and match progress.

Host the static client on GitHub Pages and the backend on a separate free Render Node web service. Configure repository-relative assets, GitHub Actions, render.yaml with plan: free, the public server URL, allowed origins, PORT, health checks and room cleanup. Keep secrets server-side. Add server-waking UI, bounded retries and reconnect backoff. Preserve local solo play when the server is unavailable. Do not use paid services or artificial uptime pings. Account for the shared 750-hour Render workspace allowance and explain that server restarts end in-memory rooms.

Work through the six phases in PLAN.md without stopping after each phase to ask whether to continue. Establish the shared simulation architecture from the outset, validate the solo vertical slice, complete Arcade fidelity, add multiplayer, test reliability, then deploy. Make routine decisions yourself and ask only for missing information or access that blocks progress.

Run meaningful simulation tests and real WebSocket integration tests. Playtest actual browser input, two and four independent clients, shared digging, competing pump attachments, falling rocks, death, round transitions, room isolation, reconnection, host departure and simulated latency. Compare screenshots and timings with the chosen arcade reference. Verify the production repository subpath and deployed online create/join flow. Fix discovered problems before calling it complete.

The delivered result should include source, finished assets, reference notes, tests, deployment configuration, controls/setup documentation, a working GitHub Pages play URL and Render health URL. Report measured validation, remaining fidelity gaps and any exact deployment step blocked by unavailable account access. Do not claim an untested deployment or perfect fidelity.
